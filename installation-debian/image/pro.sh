#!/usr/bin/env bash
set -euo pipefail

DISTRO="Debian"
MODULE="image"
MODE="pro"

echo "[INFO] ${DISTRO} | ${MODULE} | mode=${MODE}"
echo "[TODO] Tambahkan daftar paket/perintah instalasi untuk modul ${MODULE} (${MODE})"
